# 2026-06-03

## v2.6.4 规范审计 + 缺口修复

### 审计结果
对照用户完整 P0 规范（16节），发现 3 个缺口：

1. **P0**: `information_check` / `identify_extra_info` 在 LessonAction 定义了但无 handler
   - 修复：switch 中合并到 `submit_answer` 同一个 case
   - `runLessonAction` 扩展 isAnswerAction 检查覆盖这两个 action

2. **P1**: `handlePhaseBack` / `handleBackToPrevLevel` 绕过事务
   - 新增 `go_back` / `go_prev_level` action + handler
   - 消除 handlePhaseBack 的 `currentStep` 闭包陈旧问题

3. **P2**: `getDebugState()` 未集成到 UI
   - 调试面板扩展：显示 lastAction/source/phase变化/stateVersion/时间戳

### 验收
- tsc --noEmit ✅ (0 errors)
- npm run build ✅ (12 routes)
- v2.6.4 已在上轮完成核心事务系统，本轮补齐 3 个缺口

---

## v2.6.5 逻辑排序题全面修复 ✅

### 问题根因
oi_13（跑步名次题）进入 `full_solve` 后走 equation 流程：
- read → find_numbers → find_keywords → choose_operation → build_equation → answer
- 页面显示"完整破案 / 算出库存"，要求"列算式，算答案"
- 实际是逻辑排除法排序题，完全错配

### 核心改动
1. **lib/types.ts**: 新增 ProblemType, RankingAnswer, Statement, SolutionStepDetailed, AnswerType
   - Question 新增字段: problemType, requiresEquation, answerType, sceneType, themeTags, people, statements, correctRanking, solutionStepsDetailed
   - StepPhase 新增: understand_clues, logic_elimination, ranking_answer

2. **components/LogicRankingGuide.tsx** (新建 310行)
   - 5 阶段流程: read → understand_clues → logic_elimination → ranking_answer → explain
   - 排除法步骤展示 + 多选一完整排序答案
   - 无任何算式/数字输入控件

3. **lib/lessonPlanner.ts**: 动态阶段生成
   - getDefaultPhasesForStepType(type, question?) 支持 problemType 动态阶段
   - getDynamicStepTitle/getDynamicStepDescription 动态标题描述
   - normalizeStep 支持 question 参数
   - 所有步骤创建点传入 question

4. **app/play/page.tsx**: 路由 + 文案修复
   - PhaseAwareStep: 逻辑题 → LogicRankingGuide（无需改动 switch）
   - ClueSummary: 逻辑题显示👥人物 + 📝陈述 + 🧠推理
   - EquationAnswerPhase: 运行时防御 P0 断言
   - runLessonAction: ranking 答案正确性判断

5. **lib/lessonTransaction.ts**: ranking 答案校验
   - handleSubmitAnswer: answerType='ranking' 时检查完整排序
   - 4 个 callback 传入 question 参数

6. **lib/questionValidation.ts**: 6 项新校验 (18-23)
   - operation 检查, requiresEquation 检查, answerType 检查
   - correctRanking 存在检查, stepCompatibility 不包含 equation
   - 文案禁止: 库存/算式/mixed运算

7. **data/olympiadIntro.ts**: oi_13 元数据修复
   - problemType: 'logic_ranking', requiresEquation: false
   - answerType: 'ranking', people/statements/correctRanking/solutionStepsDetailed

### 验收
- tsc --noEmit ✅ (0 errors)
- npm run build ✅ (12 routes)
- 版本: v2.6.4 → v2.6.5

---

## v2.6.6 逻辑排序题校验增强 ✅

### 改动
对象: `lib/questionValidation.ts` 的逻辑题校验块（原 v2.6.5 的 6 项 → v2.6.6 的 10 项 + 1 个隐含检测）

**严重性升级（warnings → errors）**:
- #19 requiresEquation 必须为 false（原为建议级）
- #20 answerType 必须为 ranking（原为建议级，扩展匹配 `/名次/`）
- #21 correctRanking 必须存在（原为有条件检查，现无条件要求）
- #23 文案检查扩展: forbiddenTexts 加入 '糖果店'、'宠物店'，覆盖 storyTitle

**新增校验 (24-27)**:
- #24: storyTitle 含库存/糖果店/宠物店 → console.warn + 结构化日志
- #25: stepCompatibility 含 full_solve 但 requiresEquation ≠ false → warning
- #26: structuredHints.fullSteps 含完整答案（'只能是'/'所以'）→ warning
- #27: answer 为字符串但 answerType='ranking' → warning（建议用 correctRanking）

**隐含检测**:
- hasRankingKeywords 正则扩展 `谁第几`
- 新检测块: 题目含 ≥2 个逻辑排序关键词但 problemType 不是 logic_ranking → warning

### 验收
- tsc --noEmit ✅ (0 errors)
- npm run build ✅ (12 routes)
- git push origin main ✅ → Vercel auto-deploy 触发
- 版本: v2.6.5 → v2.6.6

---

## v2.6.7 P0修复: 提示提前泄露答案 ✅

### 问题
EquationAnswerPhase 默认渲染"📝 一步一步想" + solutionSteps 完整步骤，规律题直接在答题前显示目标层计算结果（如"第5层：8+2=10"），严重破坏学习目标。

### 核心改动
1. **components/lesson/HintSystem.tsx** (新增 190行)
   - 统一分层提示：light（默认）→ medium（点击）→ fullSteps（二次确认）
   - explain 阶段自动展示完整推导
   - 看完整推理需确认"会看到答案思路哦"

2. **lib/hintSafety.ts** (新增 200行)
   - textRevealsAnswer: 检测任意文案是否泄露答案（终结句/数值匹配/题型专项）
   - stepsRevealAnswer / stringStepsRevealAnswer / hintRevealsAnswer
   - renderSafePreAnswerText: 运行时防御，pre-answer 泄题返回 null

3. **app/play/page.tsx** (EquationAnswerPhase 重写)
   - solutionSteps 无条件渲染 → HintSystem 分层提示
   - 新增 getLightHint/getMediumHint/getFullStepsFromQuestion 辅助函数
   - generateSafeDefaultHint: 各题型安全默认提示
   - 答错反馈递进：第1次→第2次→第3次建议看完整推理
   - ClueSummary hints[0] 增加 hintRevealsAnswer 检测，泄题则不渲染

4. **components/LogicRankingGuide.tsx**
   - HintPanel 看完整推理增加二次确认
   - FullReasoningPanel 支持 revealsAnswer 标记，显示"(答案)"标签

5. **lib/types.ts**
   - SolutionStepDetailed 新增 revealsAnswer?: boolean
   - ProblemType 新增 'pattern'

6. **lib/questionValidation.ts** (10 项新增 #28-#37)
   - structuredHints.light / hints[0] 泄题检测 → error
   - solutionSteps / solutionStepsDetailed 默认渲染泄题检测 → error
   - pattern / ranking / age / planting / ratio 题型专项 check

7. **lib/appVersion.ts** → 2.6.7
8. **CHANGELOG.md** 更新

### 验收
- tsc --noEmit ✅ (0 errors)
- npm run build ✅ (12 routes)
- git push origin main ✅ → Vercel auto-deploy
- 版本: v2.6.6 → v2.6.7
